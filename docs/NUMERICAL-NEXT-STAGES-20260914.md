# 后续执行阶段：从数值基线到可验证波段能力

2026-09-14。承接N1–N3，不能把本轮日线窗口核对称为已经完成多周期选股策略。

## N4：证据评分的可解释候选版本（下一优先）

先在独立候选模块关闭现有源码中的RSI真实0被`or 50`替代、MA60缺窗口却借最新价填值、MACD不足样本被解释为向下、缺失DQ默认高置信度等已定位问题。加入版本化method/feature/policy ID及每组证据的贡献、缺失原因；不把规则得分映射成概率。Legacy默认路径保留，对同一批冻结输入输出差异报告，明确排名变化是候选结果。只在独立金融正确性Review与约定验证后决定默认切换。平盘RSI=100的旧约定若调整必须单独版本，不夹带进输入修复。

输出：候选证据计算器、贡献解释、原始/候选对照、回归反例；无真实样本时不提供收益优势结论。合成集仅用于正确性，不能完成样本外绩效门禁。

## N5：可用的波段观察事实与周线重采样

将周/日趋势判断与盘中T执行辅助分离。周线只能从可核验日线、市场时区/交易日历/休市事实和完整性窗口聚合；未结束周不作已收盘周使用，缺日、不同复权口径及公司行为未对齐时标记缺口。窗口仍采用交易Session或明确样本数，不把20/60/120根硬说成一个/月/季度。

输出：同一证券多尺度事实视图（而非新综合概率），失效与复核条件可解释；持仓目的不会因盘中噪声重写。真实数据足够后才做稳定性/成本/换手对照，不自动晋级。

## B1d：已落盘证据到语义ReadPort

使用B1物理内核的一致high-water，重建Event/Transport/Authority语义和查询成员集。保留原Stage3F采集链，不在普通启动中自动迁移。旧数据检查/导出先行；批量读写、流式审计和有界内存需实际测量。物理字节通过不等于Coverage/PIT/准入完成。

## B2/B3：按Case的路径与收集

每个case先回填再tail；输入成员完整消费、first-touch前的缺口检查、游标与租约、幂等恢复。后接已有Collection/Ledger，不把T操作次数变成额外独立波段样本。

## C/D：执行事实与实际运行

在现有在线计划之上单独实现手工真实fill/部分成交/费用记录及只读验证边界，计划预留/标记不能变为虚构成交。相对持有归因同时考虑未配对腿、外部现金流及公司行为；数据不足就不计算。之后进行真实多交易日Shadow和恢复验收，再推进4H可信准入及4I战绩。

## 责任及发布

默认ChatGPT直接设计、实现并执行测试；独立Reviewer不参与实现。需要本地Codex时：数值语义/PIT/持久化使用最高可用思考档，普通集成High，清单与机械复跑Medium。WorkBuddy按需做固定场景和资料索引，不常驻四会话、不决定金融正确性或发布。所有阶段先精确候选、独立Review、保护核验，再普通快进push；不覆盖旧工作树或生产DB。
