# 后续产品与数据阶段（2026-09-14）

> **N4已交付范围（2026-09-15）：** 实现`7b8816bf6b7f3dcf9272b2039c84318554a855d1`已通过独立Review和工程门禁。N4a为同输入旧规则/缺失候选只读对照及可折叠UI；N4b为声明日历周线聚合、episode去重/purge/embargo实验清单及离线CLI。未启用候选评分、训练/校准、PIT准入或实盘战绩。验证见`N4-VALIDATION-20260914.md`，后续以`N4-NEXT-STAGES-20260914.md`为准。

起点为已审查数值集成候选，最终发布SHA以handoff/DELIVERY为准。只读指标诊断不是波段模型；严格数值输入不等于策略提升。本轮未开始下列运行逻辑实现。用户已确认：数周至数月波段，持有期做T，兼有长/短持；管理持仓和发现机会并重；资金比例及自动执行未授权。

## N4a：评分缺失语义诊断，不改变生产排序

建立只读对照器，在同一不可变输入副本上记录旧评分与显式缺失处理候选：RSI=0被or替代、MA60样本不足、MACD未充分形成、DQ未知等具体场景。固定quote/bars/as_of/策略配置/公式版本和输入数量；报告差值与原因，不产生EXECUTABLE的新信号。候选不得写SignalManager/Portfolio，不用UI开关偷改评分。无数据时保留UNKNOWN，不通过平均其他指标来补齐缺失。交付固定反例、正向对照和调用图；原策略默认不变。

## N4b：多周期标签、特征与退出方案对照

分别描述SWING/LONG_TERM/SHORT_TERM的目标、决策频率、入场失效与复核窗口；日线缓存不能冒充分钟T数据，多周期不能靠重命名一个短线分数。周线只用已完成且截止时可知的数据，不把未来周收盘混入；统一价格复权、费用与可执行口径。先规则/简单基线，再评估复杂模型；时间顺序切分，去重多重试验，冻结holdout。没有适用数据证据时只交工程/诊断，不声称回测真实、校准概率或晋级。调权重必须单独版本与候选开关，默认off，用户风险阈值不代设。

## B1d：实际落盘Event/Transport的Typed ReadPort

复用现有physical_store，不再新造第三个通用Store。一次读取冻结共同high-water和身份，增量有界解码Event及Transport；semantic对象必须从真实字节重建，来源/parser/schema、观测/已知时间与覆盖证据不得由页面自报。旧Store迁移/导出显式dry-run，非空旧事实不静默升级。CLI和测试只用临时库；容量结论来自批量/内存/恢复实际测量，不靠删完整性检查提速。先离线fixtures再有限运行验证，既有采集不自动切换。

## B2/B3：持仓路径与Collection贯通

每case独立回填+tail，durable游标/幂等/重启恢复、first-touch与遗漏成员反向校验。STOP先于TARGET不能被后续好价格覆盖；过期/停牌/缺失/休市保持各自语义。Collection仅接收验证后的artifact/path/execution候选；物理完整性不自动晋级PIT或真实战绩。

## C/D：实际手工成交与使用验收

以可重放人工实际fill/partial-fill日志替代当前只标记“已开始操作”的薄状态；数量、费用、未配对腿、撤单与外部委托占用分开，记录来源与人工未验证状态，按原有同币种资金池保守处理。相对持有归因需明确统一估值时点、外部现金流和公司行为；无法重建则不出结果。最后用实际多交易日记录验收断线/恢复/数据缺口/可用性，不能用合成UI测试代替Shadow。

## 执行与审查分工

默认ChatGPT直接设计、实现、自审，再由未参与实现的只读上下文做精确commit独立Review。需要本地Codex时：PIT/原子存储/跨周期标签使用最高支持档，通常xhigh；普通接线/集成和独立Review用High；固定门禁/打包用Medium。WorkBuddy仅按需做固定fixtures、DOM测试和文档索引，无需保持四会话；不安排重复跑量。每个逻辑切片更新两个handoff、AGENTS、PRD/GapMatrix、验证记录，失败日志保留。只在适当全量门禁/独立Review/数据保护/远端祖先关系通过后普通push，禁止覆盖并行作者工作。
