# 多周期计划：既有工作收口与迭代边界

2026-09-12。本轮起点：远端 `c0c39977f4460cc9102fb3731b35cbf88786fc1d`，本地候选 `4b92c5b838c80fa3b4a2bdb719ce4f10db6410cd`，并有一份未提交的 Today 双主线测试修正。连接已经通过真实 write/edit/readback 验证。本文件更新当前状态，不将上轮未核验的最终回复当作磁盘事实。

## 1. 保留和不混入

保留：已确认的波段主画像及两条产品主线；独立的人工计划库；三种用途/核心保留量；共享旧仓、币种现金与两腿保守预留；人工取消/对账；双主线首页；相对持有的手工情景测算；原有B1物理层和W5修复。

另一个 `chatgpt-product-reassessment` 工作树的 Provider/指标/证据评分/Quant metrics 试改保持原样，不整体合并。TLS严格验证和数值输入检查有独立价值，但评分和缺窗口行为会改变排名，必须分开做冻结向量、策略版本和样本外对照；不借“重构”自动改变默认信号。

## 2. 上轮独立审查的五项闭环

| 项 | 修复边界 | 专项验收 |
|---|---|---|
| 未确认写入遇到401/403 | 保留原命令/Store ID/Origin和engine身份；认证失败不证明上次未提交；不得发往另一连接 | 丢响应→401→恢复→同ID；跨Origin不得重试 |
| 跨持仓旧预演残留 | 每次提交/输入变化先清除旧结果，校验返回输入身份，显示证券/用途/方向/限价 | A成功→B失败无确认按钮；错误member拒绝 |
| 认证和连接失效 | 私有DOM/预演清除，generation丢弃旧GET/预演/情景/写入响应；不确定命令只保留内存恢复信息 | 清凭据、离线、迟到响应与重新认证 |
| 合法旧成本精度 | 源成本作为完整十进制身份，不能按新订单金额规则截断；不支持旧行独立列issue；幂等与关闭不依赖无关父行 | 7位以上小数、异常/重复旧行、取消对账与恢复 |
| 页面停留后过期 | 服务器as-of加单调经过时间；过期检查同时取wall elapsed的保守上界；只更新标签、不抹掉输入；原预留不自动释放 | 无刷新推进时间，确认失效，预留和输入仍在 |

新浏览器工具 `qa/ui/planning_lifecycle_qa.cjs` 在真正Chromium内执行实际PlanningUI，使用合成API/Runtime，不调用外网。初次13项仅2项通过，修正后13/13；原临时真实API/browser流程24/24保持。`tests/test_planning_recovery.py` 初次6项中5项失败，修正后通过；合并领域/API/兼容测试69项通过。最终计数以本次验证附录和外置候选树证据为准。

### 第二次独立复审的两项界面补充

024c640…只读独立复审确认原五项已收敛，同时发现情景输入修改/重算失败后旧结果残留，以及UNAVAILABLE与首次NOT_CONFIGURED被混为新建库指引。现已按最小补丁关闭：情景输入/再次提交清除旧结果，异步结果绑定情景generation；原库故障要求恢复原路径与Store ID，绝不建议建立空库绕过旧预留。浏览器新反例先失败（16项中14通过），修正后16/16，原临时API流程24/24保持。最终通过仍待精确新候选独立复审。

## 3. 下一小步（先完成上述独立复审再实施）

优先增加用户可理解的“资源摘要/复核提醒”：按币种列人工现金、内部预留、余量；按实际持仓列用途、核心保留、未分类量、待复核事项。只能从现有已校验日志重算，过期余量保持未知，不从总仓推断券商可卖量；不把提醒自动变成退出信号。没有新资金比例，没有新的模型晋级，没有券商成交或账户同步。

后续主线：多周期指标/策略基线及版本比较 → B1d语义ReadPort/容量 → B2/B3路径与Collection → 手工实际fill/部分成交及归因。真实成交记录不是本轮MARK_EXECUTED或RECONCILE所能替代的；旧实现中的这两个名字只表示人工操作标记和对账关闭。

## 4. Review与发布

本轮独立审查采用未参与实现的只读Codex上下文，High思考强度，针对精确提交；禁止修改代码、连接生产SQLite、执行迁移或push。实际结果未取得前保持 REVIEW_PENDING。最终合并/推送仍以无阻断、全量测试、精确树重建、原工作区及生产DB/WAL/SHM哈希一致为门禁。无force push、reset、clean。

证据目录：`D:\Projects\stock-tracker-review\multihorizon-closure-20260912\`。所有失败尝试保留，不把历史测试数量当本轮结果。当前是 MANUAL_UNVERIFIED / MANUAL_SCENARIO，不是可成交许可或真实绩效。

## 技术参考

浏览器计时采用MDN Performance.now文档中说明的单调计时能力，同时保守考虑系统休眠时不同浏览器的差异：https://developer.mozilla.org/en-US/docs/Web/API/Performance/now 。最终有效期仍由服务器检验，浏览器不能增加有效期限。
