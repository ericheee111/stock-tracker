# 运行时生成：SQLite 数据库 + 日志（不入库，部署时运行时重建）
data/

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 虚拟环境
venv/
.venv/

# 本地环境变量 / 密钥
.env

# 编辑器 / OS
.vscode/
.DS_Store
Thumbs.db

# 安装器下载的浏览器（Playwright 开发期依赖，非运行时）
.playwright/

# Agent 工作记忆（非项目源码，绝不入库）
.workbuddy/

# Node 依赖（qa/ 下的测试工具依赖，非运行时）
node_modules/

# Playwright 可视化验证截图产物（非源码）
qa/ui/shots/
